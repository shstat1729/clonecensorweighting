% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clone_censor_weighting.R
\name{clone_censor_weighting}
\alias{clone_censor_weighting}
\title{Create a starter clone-censor-weighting dataset}
\usage{
clone_censor_weighting(data, id, follow_up, event, treatment, regimes = NULL)
}
\arguments{
\item{data}{A data frame containing one row per participant.}

\item{id}{The name of the participant identifier column.}

\item{follow_up}{The name of the follow-up time column.}

\item{event}{The name of the event indicator column.}

\item{treatment}{The name of the observed treatment column.}

\item{regimes}{Optional vector of treatment strategies to clone. When \code{NULL},
unique non-missing observed treatments are used.}
}
\value{
A tibble with one row per participant-strategy combination and the
additional columns \code{.clone_id}, \code{.regime}, \code{.censored}, and \code{.weight}.
}
\description{
Expands each observation into one row per treatment strategy and flags
immediate deviations from the observed baseline treatment as censored clones.
This is a light-weight scaffold for package development rather than a full
causal inference implementation.
}
\examples{
data(lungcancer)
cloned <- clone_censor_weighting(
  lungcancer,
  id = "id",
  follow_up = "fup_obs",
  event = "death",
  treatment = "surgery",
  regimes = c(0, 1)
)
}
